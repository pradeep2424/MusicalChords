<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="150dp"
    android:layout_height="40dp"
    android:orientation="horizontal">

    <Button
        android:id="@+id/decrement"
        android:layout_width="40dp"
        android:layout_height="match_parent"
        android:background="@drawable/bg_number_picker_left_border_blue"
        android:padding="0dp"
        android:text="—"
        android:textColor="@color/colorAccent"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/display"
        android:layout_width="60dp"
        android:layout_height="match_parent"
        android:background="@drawable/bg_number_picker_center_border_gray"
        android:focusable="false"
        android:cursorVisible="false"
        android:gravity="center"
        android:imeOptions="actionDone"
        android:inputType="number|textCapWords|textCapSentences"
        android:text="1"
        android:textColor="@color/main_text"
        android:textSize="16sp" />

    <Button
        android:id="@+id/increment"
        android:layout_width="40dp"
        android:layout_height="match_parent"
        android:background="@drawable/bg_number_picker_right_border_blue"
        android:padding="0dp"
        android:text="+"
        android:textColor="@color/colorAccent"
        android:textSize="16sp" />
</LinearLayout>